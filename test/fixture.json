{
    "fixtures": {
        "createTicket": {
            "cfg": {
                "baseUrl": "{{baseUrl}}",
                "user": "{{user}}",
                "password": "{{password}}",
                "project": "{{project}}"
            },
            "msg": {
                "body": {
                    "Ticket": {
                        "Title": "{{title}}",
                        "Type": "{{type}}",
                        "Queue": "{{queues}}",
                        "State": "{{state}}",
                        "Priority": "{{priority}}",
                        "CustomerUser": "{{customer}}",
                        "other": {
                            "test": "{{other_test}}"
                        }
                    },
                    "Article": {
                        "Subject": "{{subject}}",
                        "Body": "{{body}}",
                        "ContentType": "{{content_type}}",
                        "other": {
                            "test": "{{other_test}}"
                        }
                    },
                    "DynamicField": {
                        "Name": "{{dynamic_field_name}}",
                        "Value": "{{dynamic_field_value}}",
                        "OTHER": {
                            "UpdateMe": "{{other_update}}",
                            "Test": "{{other_test_number}}"
                        }
                    }
                }
            }
        },
        "updateTicket": {
            "cfg": {
                "baseUrl": "{{baseUrl}}",
                "user": "{{user}}",
                "password": "{{password}}",
                "project": "{{project}}"
            },
            "msg": {
                "body": {
                    "TicketID": "{{ticket_id}}",
                    "DynamicField": {
                        "Name": "{{dynamic_field_name}}",
                        "Value": "{{dynamic_field_value}}",
                        "OTHER": {}
                    }
                }
            }
        },
        "getNewTickets": {
            "cfg": {
                "baseUrl": "{{baseUrl}}",
                "user": "{{user}}",
                "password": "{{password}}",
                "project": "{{project}}",
                "queues": "{{queues}}",
                "startDateTime": "{{start_date}}",
                "includeArticles": "{{include_articles}}",
                "includeAttachments": "{{include_attachments}}"
            }
        },
        "getUpdatedTickets": {
            "cfg": {
                "baseUrl": "{{baseUrl}}",
                "user": "{{user}}",
                "password": "{{password}}",
                "project": "{{project}}",
                "startDateTime": "{{start_date}}",
                "queues": "{{queues}}",
                "includeArticles": "{{include_articles}}",
                "includeAttachments": "{{include_attachments}}"
            }
        }
    }
}
